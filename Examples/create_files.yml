# Тут мы создаём 2 файла file1.txt и file2.txt с содержимым, то что онаписано в блоке content.

---
- name: Create file1
  copy:
      dest: /home/secret/file1.txt
      content: |
        Text Line1, in file1
        Text Libe2, in file1
        Text Line3, {{ mytext }}

- name: Create file2
  copy:
      dest: /home/secret/file2.txt
      content: |
        Text line1, in file2
        Text line2, in file2
        Text line3, {{ mytext }}
